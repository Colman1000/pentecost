module.exports = {
  friendlyName: "Runs action",

  description: "",

  inputs: {},

  exits: {},

  fn: async function(inputs) {
    // await sails.helpers.helpTranslate.with({ id: 14 });
    // All done.
    return;
  }
};
